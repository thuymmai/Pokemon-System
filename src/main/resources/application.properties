spring.application.name=pokemon_system

spring.datasource.url=jdbc:mysql://localhost:3309/pokemon_system
spring.datasource.username=root
spring.datasource.password=Avocado2001}

# Configure Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# This property tells Hibernate to automatically create the database tables
# based on the JPA entities that I create in my project
spring.jpa.hibernate.ddl-auto=update

